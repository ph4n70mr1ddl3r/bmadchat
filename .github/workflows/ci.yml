name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI placeholder
        run: |
          echo "No CI steps configured for this repository."
          echo "Add language-specific build/test steps to .github/workflows/ci.yml"
